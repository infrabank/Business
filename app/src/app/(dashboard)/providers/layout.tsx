import type { Metadata } from 'next'

export const metadata: Metadata = { title: '프로바이더' }

export default function ProvidersPageLayout({ children }: { children: React.ReactNode }) {
  return children
}
